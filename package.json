{
  "name": "calendar-search",
  "version": "0.1.0",
  "private": true,
  "description": "Experimental calendar search",
  "license": "MIT",
  "author": "Theo Beers",
  "repository": "github:theodore-s-beers/calendar-search",
  "main": "./js/new-search.js",
  "scripts": {
    "format": "oxfmt",
    "lint": "oxfmt --check && oxlint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "oxfmt": "^0.46.0",
    "oxlint": "^1.61.0"
  }
}
